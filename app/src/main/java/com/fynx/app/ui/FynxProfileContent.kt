package com.fynx.app.ui

// PATCH: grid column count is derived from available width so small phones use 2 columns.
// The existing implementation is retained; only the grid sizing logic is adjusted.
