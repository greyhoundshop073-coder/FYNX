# FYNX Security Audit Scope

Audit existing implementation before changing security-sensitive code. Preserve all existing features and backend authorization. Priority areas are authentication/token handling, network transport, local secret storage, input validation, media authorization, privacy controls, AI capability/data scope, safe errors/logging, dependencies, and production build configuration.
