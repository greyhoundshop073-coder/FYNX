name: FYNX Backend CI Probe

on:
  push:
    branches: ["main"]
    paths:
      - ".github/workflows/fynx-backend-ci-probe.yml"
  workflow_dispatch:

permissions:
  contents: read

jobs:
  probe:
    runs-on: ubuntu-latest
    steps:
      - name: Verify Actions can schedule a backend workflow
        run: echo "FYNX backend CI scheduling probe passed"
